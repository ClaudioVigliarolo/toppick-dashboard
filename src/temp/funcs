  /*
  addReport(
    {
      client_id: "snswsn",
      question_id: 11661,
      reason: "bella  e interessante",
    },
    Lang.IT
  );
  */


export const sortTopicsByTimestamp = (items: Topic[]): Topic[] => {
  return items.sort(function (x, y) {
    return new Date(x.timestamp).getTime() - new Date(y.timestamp).getTime();
  });
};